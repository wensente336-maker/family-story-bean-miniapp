# Web 阶段 2 验收：API 契约适配

验收日期：2026-09-19  
确认日期：2026-09-19  
阶段状态：已确认

## 目的和作用

让 React Web 前端通过稳定的 HTTP JSON 契约连接 FastAPI，并能在开发 Mock 与真实 API 间切换，为后续登录、上传和异步处理提供统一通信底座。

## 前置依赖

- Web 阶段 1 已确认。
- FastAPI、OpenAPI 和统一错误响应可用。
- 本地 Web 与 API 服务能够分别启动。

## 交付物

- Web 环境配置和 Mock/API 数据源切换。
- 通用 HTTP API Client、自动请求追踪 ID 和标准错误对象。
- 首页 API 数据适配与失败重试页面。
- `GET /v1/home` Web 首页聚合契约。
- 可配置 CORS 白名单和 `X-Request-Id` 暴露规则。
- 更新后的 OpenAPI、前后端契约测试和环境变量模板。

## 完成标准与结果

- Web 契约测试 4 项通过。
- 后端自动化测试 22 项通过。
- TypeScript 编译及 Vite 生产构建通过。
- 浏览器真实联调完成：CORS OPTIONS 200、首页 GET 200。
- 请求追踪 ID 同时出现在响应头和响应 JSON `meta` 中。
- Mock 与真实 API 使用同一页面数据模型。
- npm 生产及开发依赖审计：0 个已知漏洞。

## 已知边界

- `/v1/home` 当前返回契约示例数据；Web 阶段 3 登录后改为当前家庭数据。
- 鉴权头、令牌恢复和家庭隔离在 Web 阶段 3 接入前端。
- 文件上传与对象存储不属于本阶段。
- 后端测试仍有一条 Starlette 测试组件弃用警告，不影响运行结果。

## 确认点

用户已确认 Mock/API 切换方式、HTTP 契约、错误格式和联调结果，Web 阶段 3 已启动。
